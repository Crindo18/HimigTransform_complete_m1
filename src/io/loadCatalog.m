function catalog = loadCatalog(Cfg)
%LOADCATALOG Read db/catalog.csv into a typed table.
%
%   Restores the categorical and integer types that writetable flattens to
%   text, so downstream grouping behaves consistently.
%
%   Milestone: M0.  Blueprint: section(s) 2.2.
%
%   STATUS: stub. The contract above is frozen (blueprint section 5); the body
%   is written at the milestone named above.
%
%   See also BUILDCATALOG.

error('HimigTransform:NotImplemented', ...
    'loadCatalog is a stub (Milestone M0). See docs/designNotes.md.');

end
